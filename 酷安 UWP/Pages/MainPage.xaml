<Page
    x:Class="CoolapkUWP.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fcu="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract,5)"
    xmlns:cu="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractNotPresent(Windows.Foundation.UniversalApiContract,5)"
    xmlns:local="using:CoolapkUWP.Pages"
    xmlns:control="using:CoolapkUWP.Control"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <control:DatatemplatesDictionary/>
            </ResourceDictionary.MergedDictionaries>
            <local:HamberTemplateSelector x:Key="hamsele" DataTemplate1="{StaticResource DefaultTemplate}" DataTemplate2="{StaticResource SearchTemplate}"/>
            <DataTemplate x:Key="DefaultTemplate" x:DataType="local:MenuItem">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48" />
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="{x:Bind Icon}"/>
                    <Ellipse Margin="10,0,0,0" Width="30" Height="30" Grid.RowSpan="2" HorizontalAlignment="Left" >
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding Image}"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Grid.Column="1" Text="{x:Bind Name}" FontSize="16" VerticalAlignment="Center"/>
                </Grid>
            </DataTemplate>
            <DataTemplate x:Key="SearchTemplate" x:DataType="local:MenuItem">
                <AutoSuggestBox Margin="10,0" VerticalAlignment="Center" QueryIcon="Find" Foreground="{ThemeResource ApplicationForegroundThemeBrush}" ItemTemplateSelector="{StaticResource tspl}"
                                    TextChanged="AutoSuggestBox_TextChanged" QuerySubmitted="AutoSuggestBox_QuerySubmitted"/>
            </DataTemplate>
            <Style x:Key="HamburgerMenuItemStyle" TargetType="ListViewItem">
                <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="MinHeight" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="UseSystemFocusVisuals" Value="True"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Grid x:Name="LayoutRoot" Background="{TemplateBinding Background}" Height="40" Control.IsTemplateFocusTarget="True">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="PointerStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource SystemControlHighlightListLowBrush}"/>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightAltBaseHighBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource SystemControlHighlightListMediumBrush}"/>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightAltBaseHighBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Selected">
                                            <VisualState.Setters>
                                                <Setter Target="SelectionIndicator.Opacity" Value="1.0"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="PointerOverSelected">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource SystemControlHighlightListLowBrush}"/>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightAltBaseHighBrush}"/>
                                                <Setter Target="SelectionIndicator.Opacity" Value="1.0"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="PressedSelected">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource SystemControlHighlightListMediumBrush}"/>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightAltBaseHighBrush}"/>
                                                <Setter Target="SelectionIndicator.Opacity" Value="1.0"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="DisabledStates">
                                        <VisualState x:Name="Enabled"/>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Opacity" Value="{ThemeResource ListViewItemDisabledThemeOpacity}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Grid HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Rectangle x:Name="SelectionIndicator" Fill="{ThemeResource SystemControlForegroundAccentBrush}" Height="24" Opacity="0.0" Width="6"/>
                                </Grid>
                                <ContentPresenter x:Name="ContentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentTransitions="{TemplateBinding ContentTransitions}" Grid.Column="1" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <ControlTemplate x:Key="HamburgerMenuTemplate1" TargetType="controls:HamburgerMenu">
                <Grid>
                    <SplitView x:Name="MainSplitView" CompactPaneLength="{TemplateBinding CompactPaneLength}" DisplayMode="{TemplateBinding DisplayMode}" IsTabStop="False" IsPaneOpen="{Binding IsPaneOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" OpenPaneLength="{TemplateBinding OpenPaneLength}" PanePlacement="{TemplateBinding PanePlacement}" 
                               cu:PaneBackground="{ThemeResource SystemControlPageBackgroundChromeLowBrush}" fcu:PaneBackground="{ThemeResource SystemControlChromeLowAcrylicWindowBrush}">
                        <SplitView.Pane>
                            <Grid x:Name="PaneGrid" FlowDirection="LeftToRight" Margin="0,32,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Height="{TemplateBinding HamburgerHeight}" Margin="0,0,0,8" Grid.Row="0"/>
                                <ListView x:Name="ButtonsListView" ItemContainerStyle="{StaticResource HamburgerMenuItemStyle}" ItemTemplate="{TemplateBinding ItemTemplate}" IsItemClickEnabled="True" ItemsSource="{TemplateBinding ItemsSource}" ItemTemplateSelector="{TemplateBinding ItemTemplateSelector}" AutomationProperties.Name="Menu items" Grid.Row="1" SelectedItem="{Binding SelectedItem, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" SingleSelectionFollowsFocus="False" SelectedIndex="{Binding SelectedIndex, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" TabIndex="1" ScrollViewer.VerticalScrollBarVisibility="Auto" Width="{TemplateBinding OpenPaneLength}"/>
                                <ListView x:Name="OptionsListView" ItemContainerStyle="{StaticResource HamburgerMenuItemStyle}" ItemTemplate="{TemplateBinding OptionsItemTemplate}" IsItemClickEnabled="True" ItemsSource="{TemplateBinding OptionsItemsSource}" ItemTemplateSelector="{TemplateBinding OptionsItemTemplateSelector}" Margin="0,20,0,8" AutomationProperties.Name="Option items" Grid.Row="2" SelectedItem="{Binding SelectedOptionsItem, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" SingleSelectionFollowsFocus="False" SelectedIndex="{Binding SelectedOptionsIndex, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" TabIndex="2" VerticalAlignment="Bottom" ScrollViewer.VerticalScrollBarVisibility="Disabled" Width="{TemplateBinding OpenPaneLength}"/>
                            </Grid>
                        </SplitView.Pane>
                        <ContentPresenter x:Name="ContentPart" Content="{TemplateBinding Content}" AutomationProperties.Name="Content"/>
                    </SplitView>
                    <Button x:Name="HamburgerButton" fcu:Style="{ThemeResource ButtonRevealStyle}" Background="Transparent" BorderThickness="0" Height="{TemplateBinding HamburgerHeight}" Margin="0,32,0,8" AutomationProperties.Name="Main button" Padding="0" TabIndex="0" VerticalAlignment="Top" Visibility="{TemplateBinding HamburgerVisibility}" Width="{TemplateBinding HamburgerWidth}">
                        <ContentControl ContentTemplate="{TemplateBinding HamburgerMenuTemplate}" Foreground="{TemplateBinding PaneForeground}" IsTabStop="False" Margin="{TemplateBinding HamburgerMargin}"/>
                    </Button>
                </Grid>
            </ControlTemplate>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:Name="GridRootLayout" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <!--平板-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="769"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="hamburgerMenuControl.DisplayMode" Value="CompactInline"/>
                        <Setter Target="hamburgerMenuControl.HamburgerVisibility" Value="Visible"/>
                        <Setter Target="hamburgerMenuControl.IsPaneOpen" Value="True"/>
                        <Setter Target="TopBar.Height" Value="0"/>
                        <Setter Target="BottomNavBar.Height" Value="0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <controls:HamburgerMenu Template="{StaticResource HamburgerMenuTemplate1}" HamburgerVisibility="Collapsed" x:Name="hamburgerMenuControl" ItemTemplateSelector="{StaticResource hamsele}" ItemClick="OnMenuItemClick" 
                                OptionsItemTemplate="{StaticResource DefaultTemplate}" OptionsItemClick="OnMenuItemClick" SelectedIndex="0"
                                DisplayMode="Overlay" PaneForeground="{ThemeResource ApplicationForegroundThemeBrush}">
            <Grid>
                <Frame Grid.Row="1" x:Name="VFrame"/>

                <Grid x:Name="TopBar" Height="48" VerticalAlignment="Top" Margin="0,32,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Background="Transparent" cu:Style="{StaticResource ButtonStyle1}" fcu:Style="{ThemeResource ButtonRevealStyle}"
                            VerticalAlignment="Bottom" Height="48" Width="48" x:Name="UserButton" Click="UserButton_Click">
                        <Grid>
                            <SymbolIcon Symbol="Contact" Foreground="{ThemeResource ApplicationForegroundThemeBrush}"/>
                            <Ellipse Width="28" Height="28">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding Image}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                        </Grid>
                    </Button>
                    <AutoSuggestBox Margin="2,0,15,8" Height="32" Grid.Column="1" VerticalAlignment="Bottom" QueryIcon="Find" ItemTemplateSelector="{StaticResource tspl}"
                                    TextChanged="AutoSuggestBox_TextChanged" QuerySubmitted="AutoSuggestBox_QuerySubmitted"/>
                </Grid>
                <Grid Grid.Row="1" VerticalAlignment="Bottom" x:Name="BottomNavBar" Margin="0,2,0,0" 
                                    cu:Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}" fcu:Background="{ThemeResource SystemControlChromeHighAcrylicElementMediumBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Height="48" cu:Style="{StaticResource BottomNavButtonStyle}" fcu:Style="{ThemeResource ButtonRevealStyle}" VerticalContentAlignment="Center"
                            x:Name="rect1" VerticalAlignment="Top" Background="Transparent" HorizontalAlignment="Stretch" Click="AppBarButton_Click" Tag="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <SymbolIcon Symbol="Home"/>
                            <TextBlock Grid.Row="1" FontSize="12">首页</TextBlock>
                        </Grid>
                    </Button>
                    <Button Height="48" cu:Style="{StaticResource BottomNavButtonStyle}" fcu:Style="{ThemeResource ButtonRevealStyle}" VerticalContentAlignment="Center"
                            x:Name="rect2" Background="Transparent" VerticalAlignment="Top" Click="AppBarButton_Click" Tag="2" Grid.Column="1" HorizontalAlignment="Stretch">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <SymbolIcon Symbol="Shop"/>
                            <TextBlock Grid.Row="1" FontSize="12">应用游戏</TextBlock>
                        </Grid>
                    </Button>
                    <Button Height="48" cu:Style="{StaticResource BottomNavButtonStyle}" fcu:Style="{ThemeResource ButtonRevealStyle}" VerticalContentAlignment="Center"
                            x:Name="rect3" Click="AppBarButton_Click" VerticalAlignment="Top" Background="Transparent" Tag="0" Grid.Column="2" HorizontalAlignment="Stretch">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <SymbolIcon Symbol="Setting"/>
                            <TextBlock Grid.Row="1" FontSize="12">设置</TextBlock>
                        </Grid>
                    </Button>
                </Grid>
            </Grid>
        </controls:HamburgerMenu>
    </Grid>
</Page>
