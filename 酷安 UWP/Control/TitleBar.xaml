<UserControl
    x:Class="CoolapkUWP.Control.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolapkUWP.Control"
    xmlns:cu="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractNotPresent(Windows.Foundation.UniversalApiContract,5)"
    xmlns:fcu="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract,5)"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:CoolapkUWP.Data"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="titleGrid" Height="{x:Bind data:Settings.PageTitleHeight}" VerticalAlignment="Top" 
          cu:Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}" fcu:Background="{ThemeResource SystemControlChromeHighAcrylicElementMediumBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="48"/>
        </Grid.ColumnDefinitions>
        <!--<Button Style="{StaticResource NavigationBackButtonNormalStyle}" Height="48" Width="48" x:Name="BackButton" Click="BackButton_Click" VerticalAlignment="{x:Bind data:Settings.titleContentVerticalAlignment}"/>-->
        <Button Grid.Column="0" x:Name="BackButton" Click="BackButton_Click" Style="{StaticResource ButtonStyle1}" Width="48" Height="48" VerticalAlignment="{x:Bind data:Settings.titleContentVerticalAlignment}">
            <SymbolIcon x:Name="BackButtonIcon" Symbol="Back"/>
        </Button>
        <TextBlock Grid.Column="1" Margin="{x:Bind data:Settings.titleTextMargin}" x:Name="title" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="{x:Bind data:Settings.titleContentVerticalAlignment}"/>
        <Button Grid.Column="2" x:Name="RefreshButton" Click="RefreshButton_Click" Style="{StaticResource ButtonStyle1}" Width="48" Height="48" VerticalAlignment="{x:Bind data:Settings.titleContentVerticalAlignment}">
            <SymbolIcon Symbol="Refresh"/>
        </Button>
    </Grid>
</UserControl>
