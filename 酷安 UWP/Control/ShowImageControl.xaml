<UserControl
    x:Class="酷安_UWP.ShowImageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:酷安_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    KeyUp="UserControl_KeyUp">

    <Grid>
        <FlipView Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" ScrollViewer.IsZoomChainingEnabled="True" ScrollViewer.BringIntoViewOnFocusChange="False" x:Name="SFlipView">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <ScrollViewer x:Name="ScrollViewerMain" ZoomMode="Enabled"
                                          MinZoomFactor="0.5"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden">
                        <Image Source="{Binding}"
                                       MaxWidth="{Binding Path=ViewportWidth,ElementName=ScrollViewerMain}"
                                       MaxHeight="{Binding Path=ViewportHeight,ElementName=ScrollViewerMain}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                    </ScrollViewer>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
        <Button x:Name="CloseFlip" HorizontalAlignment="Right" VerticalAlignment="Top" Click="CloseFlip_Click" Width="48" Height="48">
            <SymbolIcon Symbol="Cancel"/>
        </Button>
        <Button x:Name="SaveImage" Click="CloseFlip_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,48,0" Width="48" Height="48">
            <SymbolIcon Symbol="Download"/>
        </Button>
        <Grid VerticalAlignment="Center" x:Name="DownloadStatePresenter">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{x:Bind DownloadStatePresentProgressBar.Value,Mode=OneWay}"/>
                <TextBlock>%</TextBlock>
            </StackPanel>
            <ProgressBar Grid.Row="1" Margin="50,0" x:Name="DownloadStatePresentProgressBar" Maximum="100"/>
        </Grid>
    </Grid>
</UserControl>
